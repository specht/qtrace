# this is Windows ready now!!
projects:
  - projects/qtrace
qt: [core, xml, svg]
win32Libs: []
versionInject:
  src/version.h: |
    #pragma once
    #include <QtCore>
    QString gs_Version = "#{VERSION}";
